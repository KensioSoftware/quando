/**
 * Quando: declarative temporal rules for schedules, deadlines, constraints and
 * exceptions.
 *
 * A placeholder, and the first thing the design work replaces. It is here so
 * that the build, the type check, the linter and the test run all have
 * something to act on — a toolchain that has never run over a single file is
 * not known to work, and finding out otherwise is much cheaper now than on the
 * first real pull request.
 */

/** The package name. Not part of the API; see the note above. */
export const packageName = "@kensio/quando";
