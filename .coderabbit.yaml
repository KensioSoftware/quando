# yaml-language-server: $schema=https://docs.coderabbit.ai/schemas/coderabbit-schema.json
---
# Repository level CodeRabbit settings, which override the organisation ones for
# this repository only.
reviews:
  auto_review:
    # Do not review on opening a pull request, and do not review again on every
    # push after that. Ask for a review with an `@coderabbitai review` comment
    # on the pull request when one is wanted.
    enabled: false
    auto_incremental_review: false
